# PROJECT_STATE.md

> 本文件是 Yuli运营后台 的唯一开发记忆文件。
> 以后每次新会话开始时，必须先阅读本文件，再进行任何开发、修复、构建或部署。

## 项目名称

Yuli运营后台

## 当前模块

1. 运营总览（含每月 31 条上新目标与折线图）
2. 商品折扣（活动折扣、同事售价参考）
3. 运营数据
4. 商品档案
5. 运营任务

## 已删除模块

1. 爆款拆解中心
2. 标题工坊
3. 提示词工坊
4. AI识图助手
5. 提示词素材库
6. 我的规则中心

## 商品档案规则

商品档案是折扣记录和同事售价参考的商品基础数据源，不区分 AG、DS、HX 店铺。

当前基础数据来自《灯饰产品价格表（20260818）》：

- 143 个商品
- 293 个规格
- 119 个灯串商品
- 24 个地插灯商品
- 保留原价格表的 15 个细分类

每个规格保留规格名、成本价、包装尺寸、单品重量、包装重量和装箱数。原表图片列为不兼容公式，网站暂不导入图片。

商品档案页面直接使用 Excel 黄色填充的 15 个分类标题作为筛选入口；不再用“灯串 / 地插灯”作为页面筛选，也不重复显示粗分类列。

发布该版本时只替换旧商品档案，必须保留折扣记录、同事售价、运营数据、运营任务和上新记录。使用 `productCatalogVersion` 确保同一版本只替换一次，之后用户的新增、修改和删除都正常保存。

商品档案必须支持新增、修改和删除商品，也必须支持新增、修改和删除规格。

商品整体推荐档位采用最保守规格原则。

档位顺序：

1. 8折
2. 8.5折
3. 9折
4. 不可报活动

示例：

SKU1 = 8折

SKU2 = 8.5折

商品整体 = 8.5折

未上架或不参与活动计算的SKU，不参与商品整体推荐档位计算。

## 折扣输入规则

8折输入：

8

不是：

0.8

不是：

20

8.5折输入：

8.5

9折输入：

9

系统自动换算活动价。

活动价自动计算。

禁止手动输入活动价。

## 云同步规则

Supabase为主。

LocalStorage为备份。

禁止云端数据被示例数据覆盖。

如果云端数据为空，禁止自动覆盖本地真实数据。

如果本地有真实数据、云端为空，应提示或保留本地数据，不能静默清空。

## 部署规则

GitHub Pages 使用 `gh-pages` 分支部署。

push 到 `main` 后，`.github/workflows/pages.yml` 自动执行：

1. 安装依赖
2. 注入 Supabase 构建环境变量后执行 `npm run build`
3. 将 `dist` 内容发布到 `gh-pages` 分支

GitHub Secrets 或 GitHub Variables 必须配置：

- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_ANON_KEY`

workflow 会优先读取 Secrets，缺失时读取 Variables。

如果 GitHub 未配置 Secrets/Variables，workflow 使用 `.env.example` 中已公开的 Supabase URL 和 publishable anon key 作为构建兜底。

如果构建环境缺少任一变量，workflow 必须失败，禁止静默发布 Supabase 未配置版本。

前端只读取：

- `import.meta.env.VITE_SUPABASE_URL`
- `import.meta.env.VITE_SUPABASE_ANON_KEY`

禁止再使用旧的 GitHub Pages Actions artifact 部署方式：

- `actions/upload-pages-artifact`
- `actions/deploy-pages`

## 快速活动测算规则

快速活动测算是独立临时报活动工具。

用于不需要录入产品信息库、不需要创建店铺商品档案的临时商品。

禁止：

- 读取产品信息库
- 读取店铺商品档案
- 保存到商品档案
- 同步到Supabase

只允许基于当前页面输入项即时计算。

输入项：

- 供货价
- 原售价
- ROAS
- 目标利润（默认3元）
- 预留广告费（默认3元）

自动计算：

- 8折利润
- 8.5折利润
- 9折利润
- 最低活动价
- 保本活动价
- 保本折扣
- 建议折扣档

## 当前已知BUG

1. 店铺商品档案中，报活动计算弹窗的折扣输入框占位符仍残留乱码：`折扣??8 / 0.8 / 80`。后续需要改成正常中文：`折扣，如8 / 0.8 / 80`。
2. `PROJECT.md` 中记录过历史乱码修复事项，后续如发现 `?`、Unicode转义、乱码中文，需要优先清理。

## 下次开发前

必须先阅读 `PROJECT_STATE.md`。

开发时必须遵守本文件规则。

如果业务规则发生变化，必须同步更新本文件。
